# Frontend — AI House Plan Generator

React frontend for the house plan generator.

## Setup

```bash
npm install
npm start
```

Opens at http://localhost:3000. Requires the Flask backend running on port 5000.
